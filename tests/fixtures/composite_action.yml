name: My Composite Action
description: "A composite action that sets up the environment"
runs:
  using: composite
  steps:
    - uses: actions/setup-python@v5
    - run: pip install -r requirements.txt
      shell: bash
